import { Manrope, Playfair_Display } from "next/font/google";

export const inter = Manrope({
  subsets: ["latin"],
  display: "swap",
});

export const roboto_mono = Playfair_Display({
  subsets: ["latin"],
  display: "swap",
});
